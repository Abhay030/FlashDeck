import { PricingPageContent } from "@/components/features/pricing/PricingPageContent";

export const metadata = {
  title: "Pricing",
  description: "Simple, transparent pricing for AI flashcards — full feature access in the demo while we ship billing.",
};

export default function PricingPage() {
  return <PricingPageContent />;
}
